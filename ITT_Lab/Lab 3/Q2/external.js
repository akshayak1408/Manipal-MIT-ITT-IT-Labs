var sent = document.getElementById('sentence').value;
var res = sent.split(" ");
var leng = res.length;
var i = 0;
function convert_to_pig_latin() {
    for(i=0;i<leng;i++)
    {
        if(res)
    }
}

function pig_word()
{
    var str=inp.value;
    a=str.charAt(0);
    str=str.substr(1,str.length-1);
    str=str+a+"ay";

    
    p.innerText=str;
}
